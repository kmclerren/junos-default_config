#jinja2: lstrip_blocks: True
chassis {
    redundancy {
        graceful-switchover;
    }
    aggregated-devices {
        ethernet {
            device-count 50;
        }
    }
    alarm {
        management-ethernet {
            link-down ignore;
        }
    }
}